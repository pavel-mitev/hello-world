//Тази я направих директно в сайта и ми минаха тестовете и реших да я кача така.
/*
  Remove all duplicate elements from a sorted linked list
  Node is defined as 
  struct Node
  {
     int data;
     struct Node *next;
  }
*/
Node* RemoveDuplicates(Node *head)
{
    if(head == NULL) //Проверяваме дали е празен списъка, ако е го оставяме така.
        return NULL;
    Node* p = head; //Копираме си началото на списъка за да можем да го върнем накрая.
    while(p->next) //Обхождаме елементите на списъка. Цикълът ще спре когато следващият подред указател стане NULL.
    {
        if(p->data == p->next->data) //Ако текущия и следващия елемент имат равни стойности.
        {
            p->next = p->next->next; //местим текущия да сочи към по-следващия, така изчезва от свързъния списък повтарящия се елемент.
        }
        else
            p = p->next; //Ако няма повтарящ се просто минаваме на следващия елемент.
    }
    return head; //Връщаме началото на списъка.
}
